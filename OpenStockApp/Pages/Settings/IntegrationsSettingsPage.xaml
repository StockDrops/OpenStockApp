<?xml version="1.0" encoding="UTF-8"?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vs="clr-namespace:OpenStockApp.Views.Settings"
             x:Class="OpenStockApp.Pages.Settings.IntegrationsSettingsPage"
             
             Title="Integrations"
            >
    <!--BackgroundColor="{AppThemeBinding Dark={StaticResource BackgroundColorDark}, Light={StaticResource BackgroundColor}}"-->
    <!--BackgroundColor="{AppThemeBinding Dark={StaticResource BackgroundColorDark}, Light={StaticResource BackgroundColor}}"-->
    <Grid Margin="{StaticResource SmallLeftMargin}">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <ScrollView>
            <VerticalStackLayout Grid.Row="0">
                <vs:DiscordSettingsView/>
                <!--<vsettings:EmailSettingsView/>-->
                
            </VerticalStackLayout>
        </ScrollView>
    </Grid>


</ContentPage>